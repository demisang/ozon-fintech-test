# Server configurations
server:
  host: "0.0.0.0"
  port: 8382

# Storage "db" or "memory"
storage: "memory"
shortLinkLength: 10

# Database credentials
database:
  host: "db"
  port: "5432"
  user: "user"
  pass: "12345"
  dbName: "ozon_fintech"
